# RL 과제 Lessons Learned

> garage_rl (PPO Garage Scheduling, level 3) 실전 경험 정리.
> 다음 RL 실습 시작 시 이 문서를 먼저 훑고 계획을 세울 것.

---

## 0. 최종 결과 요약 (경험의 근거)

- 문제: 정비소 3개 배치 스케줄링, 인내도 30틱, 매 틱 도착. baseline 1490.8.
- 판정: Pass ≤ 1300, Distinguished ≤ 1050, 참고 최고 950.
- **최종 제출**: 1223.4 (baseline 대비 +17.9%, Pass 확보, Distinguished 미달)
- 총 소요 시간: 계획 30분 + 구현 20분 + 학습 sweep 4회 × ~15분 + 평가/리포트 30분
- 학습 곡선 요약 (동일 obs+reward, 각 500k step, ent만 변경, 3 seed 평균):

| 설정 | score 평균 | 판정 |
|---|---|---|
| ent=0.05, R_FINISH=0 | 1419 | 미달 |
| ent=0.05, R_FINISH=0.05 | 1349 | Pass 진입 |
| ent=0.00, R_FINISH=0.05 | 1256 | **Pass, best 1223** |

---

## 1. 계획 단계에서 가장 중요한 것

### 1.1 "무엇을 최소화하는지"와 "보상"의 정렬을 먼저 못박기

**평가 지표(점수)와 보상의 스케일을 비례식으로 맞춘다.**
- 점수 = `A × 틱 + B × 이탈` 형태라면 보상도 `-α × 매틱 - β × 이탈`로,
  `α : β = A : B` 비율을 유지해야 한다.
- 이 비율이 어긋난 sweep 축(예: 이탈 페널티만 세게 키우기)은 **지표와 다른 목표**를
  학습하게 되므로 후보에서 배제한다.
- 지표에 없는 보너스(assigned, done, finish)는 **보상 해킹 위험**이 있다:
  - `done`: 종료가 항상 발생하는 문제라면 상수 신호 → 정보량 0
  - `assigned`: "일단 아무 데나 빨리 넣기"를 강화 → 오배치 유도
  - `finished`: throughput 유도용으로 **필요한 경우가 있음** (§3 참고)

### 1.2 절대 규칙과 수정 가능 파일을 문서 상단에 못박기

- "수정 가능한 파일" 리스트를 §0에 명시. 검증(hash) 대상은 특히 강조.
- **파일 공유 제약** (예: `solution.py` 하나가 문제 1/2/3 공용) → 레벨별 하드코딩 금지.
  이 제약은 관측/보상 설계에 근본적 영향. sweep 병렬화 가능 여부도 결정.

### 1.3 시간 예산부터 세우기 (time-box)

- 총 가용 시간 → 학습 예산 vs 리포트 예산으로 먼저 분리.
- **Fallback 우선 원칙**: 가장 낮은 기준(Pass)을 만족하는 모델을 최대한 빨리 확보,
  이후 시간이 남는 만큼만 최적화 시도. 이번 과제에서 이게 결정적이었다.
- 학습 시간 실측을 초기에: 1스레드 500k = 12~15분 (병렬 3개도 비슷).

---

## 2. 관측(Observation) 설계

### 2.1 "정보가 없으면 학습 불가"라는 명제

- 관측에 없는 정보는 정책이 학습으로 발견할 방법이 없다.
- 문제 조건(예: C 정비소는 크기≤4에 유리)을 정책이 학습하려면 **크기 자체**를 관측에
  포함해야 한다. 규칙을 하드코딩하지 않고도, 정책이 스스로 찾아낸다.

### 2.2 "선형 변환으로 얻는 파생 피처"는 낭비

- MLP는 즉시 선형 변환을 학습한다. `1 - patience_norm`, `1 - size_norm` 같은 파생값을
  관측에 추가해봐야 정보량 0.
- **새로운 정보를 주는 파생만 채택**: 이번 과제에서 유효했던 것은
  - `station_rem_ticks` = 남은 절대 틱 (진행률은 비율이라 "몇 틱 남았는지" 없음)
  - `patience_abs` = 같은 단위로 정규화 → "정비소가 비기 전에 차가 떠나는가"를
    신경망이 직접 비교 가능
- 판단 기준: **기존 feature의 선형 조합으로 표현 불가한가?** 예 → 채택, 아니오 → 기각.

### 2.3 정규화 스케일의 단위 통일

- 서로 비교되어야 할 두 값이 다른 단위라면 학습이 불리하다.
  이번 예: `patience_norm`(레벨 상대값)과 진행률은 비교 불가 →
  `patience_abs` (틱/50)와 `station_rem_ticks`(틱/50)을 추가해 같은 단위로 맞춤.
- [0,1] 범위 강제라면 clip을 명시 (`min(1.0, ...)`).

---

## 3. 보상(Reward) 설계

### 3.1 지표-보상 정렬이 근본, 그러나 "학습 신호"는 추가로 필요

- 지표 정렬만으로는 **탐색이 지연**될 수 있다. 이번 과제에서 지표 정렬만 쓴 모델
  (R_FINISH=0)은 이탈은 잘 피했지만 **너무 오래 대기**해서 steps가 폭증 (798 vs baseline 710).
- `R_FINISH = 0.05` (완료당 소량 양수) 추가로 처리량 신호를 주자 steps가 압축되어
  점수가 100점 이상 개선.
- 원칙: **지표 정렬 = 목적함수, 추가 신호 = 학습 편의**. 추가 신호는 지표를 왜곡하지
  않는 방향(throughput ↑ = 대기시간 ↓ = 점수↓)이어야 한다.

### 3.2 진단은 "무엇을 트레이드했는가"로 본다

- 점수는 여러 성분의 합 → 개선/악화 시 성분별로 분해해서 어디가 나빠졌는지 본다.
  이번 예: baseline vs v2c → removed는 개선(15.6→12.0), steps는 악화(710→798).
  → 이탈 억제는 성공, 대신 대기 과잉. R_FINISH로 해결.
- test.py의 `steps` / `removed` / `serviced`를 항상 함께 기록.

### 3.3 극단값 금지

- 원본 문서의 예시 `-100` 같은 극단값은 학습을 불안정하게 함.
- 값의 스케일은 좁게 (`~O(1)`), 부호와 비율로 신호 준다.

---

## 4. 하이퍼파라미터 튜닝

### 4.1 관측이 풍부하면 `ent_coef`를 낮춰라

- **이번 과제 가장 큰 배움**: rich한 관측(27차원, 정규화된 fitness feature 포함)에
  인내도 30틱처럼 **오배치 페널티가 즉각 크게 오는 환경**에서는 탐색 노이즈가 오히려 해롭다.
- ent_coef 실측 (동일 조건, 3 seed):
  - 0.05 → 1349 (탐색 노이즈로 오배치 유발)
  - 0.00 → 1256 (**-93점 개선**, best 1223)
- 판단 기준:
  - 관측이 sparse하거나 매칭 규칙이 숨겨져 있음 → ent 0.01 이상 (탐색 필요)
  - 관측이 rich하고 오배치가 즉각 큰 벌점 → ent 0.0 (탐색 노이즈 억제)

### 4.2 `gamma`는 신호 지연 길이와 맞추기

- 이번 과제 이탈 신호 지연 = 인내도 틱 수 (30 or 100). 기본 0.99의 유효 horizon(~100틱)이
  경계에 있음 → 0.995로 소폭 상향은 유효.
- 다만 **0.999는 R_FINISH 같은 양수 보너스와 결합 시 발산** (미래 finish 보너스 과대평가
  → 이번 과제에서 점수 2582로 붕괴). 양수 보너스를 넣을 때는 gamma를 낮게.

### 4.3 grid search는 낭비, 1-factor 순차 sweep

- 5개 축 각 3후보 = 243 조합. 실제로는 1축씩 best 채택하며 진행해도 충분.
- 실측 순서 우선순위: gamma → ent_coef → learning_rate → n_steps → batch/epochs.

### 4.4 seed 분산은 항상 크다 — 최소 3 seed 확인

- 이번 과제 동일 config 다른 seed: 1223 ~ 1322 (변동폭 100점).
- 단일 seed 성적으로 판단 금지. 시간이 없으면 **best-of-3**을 채택.

---

## 5. 실행 인프라

### 5.1 병렬 실행의 함정

- 1스레드 PPO 3개 병렬 → 각 스레드가 CPU 경쟁으로 스루풋 저하 (668→577 steps/s).
  단일보다 총 처리량은 이득이지만 **한 개당 시간은 늘어남**.
- 병렬 실행 시 반드시 **경로 분리** (`model_dir`/`tensorboard_dir` 서로 다르게).
  안 하면 `ppo.zip` 서로 덮어씀.
- 파일 공유 제약(예: solution.py) 때문에 **관측/보상 실험은 병렬 불가**, HP sweep만 병렬화.

### 5.2 백그라운드 실행 방식

- 세션 종료 시 사라지는 백그라운드는 위험. **PowerShell `Start-Process -WindowStyle Hidden
  -RedirectStandardOutput`** 방식이 안전.
- 로그 파일을 tail하여 진행 확인 (콘솔 attach 불필요).

### 5.3 학습 시간 실측을 초기에

- 짧은 timesteps(100, 10k)로 steps/s 실측 → 실제 예산 계획에 반영.
- 이번 실측: 단일 668 steps/s, 3병렬 577 steps/s (약 14% 저하).

### 5.4 "제출물 경로"는 미리 확인 후 정확히 저장

- 이번 과제 제출 경로: `model/level3/ppo.zip` (정확히 이 경로).
- 실험용 모델은 별도 폴더(`model_l3v3_a` 등)에 저장, best만 정식 경로로 복사.
- **Fallback 즉시 확보 후 최적화 계속** — 시간 초과 대비.

---

## 6. 실험 관리

### 6.1 EXPERIMENTS.md에 표로 축적

- 각 run: obs 버전 / reward 파라미터 / config / seed / test score / steps / removed / vs baseline.
- **test.py(고정 시드 100 에피소드) 값만 공식 기록.** train 로그의 `점수추정`(5 에피소드)은
  참고용, 판단 근거로 삼지 말 것.

### 6.2 실패한 실험도 근거와 함께 문서화

- "왜 안 통했는가"를 남기지 않으면 나중 시도에서 반복. 이번 예: γ=0.999 + R_FINISH 조합 붕괴.

### 6.3 리포트를 학습 진행 중에 작성 시작

- 관측/보상 설계는 학습 결과와 무관하게 확정 → **먼저 작성**.
- 결과 표만 마지막에 채움. 마감 임박해서 처음부터 쓰기 시작하면 실패.

---

## 7. 다음 실습 착수 체크리스트

1. [ ] 평가 지표를 수식으로 명확히 기술 (`점수 = ...`, 낮을수록/높을수록)
2. [ ] 수정 가능한 파일 + 검증 대상 파일 + 파일 공유 제약 문서화
3. [ ] 마감까지 남은 시간 → 학습 예산 / 리포트 예산으로 분리
4. [ ] 관측 후보 나열: **새로운 정보인지** 각각 판정 (선형 변환은 배제)
5. [ ] 보상: 지표와 비율 정렬 + 필요 시 학습 편의용 보조 신호 (throughput 등)
6. [ ] 학습 시간 실측 후 sweep 범위 결정
7. [ ] Fallback(최소 통과 기준) 모델 조기 확보 → 이후 최적화
8. [ ] HP sweep: gamma → ent_coef 순서, 각 3 seed 확인
9. [ ] 관측 rich → ent_coef 낮게 시도, 관측 sparse → ent_coef 높게 시도
10. [ ] 리포트를 학습 시작과 동시에 작성 시작 (결과 표만 마지막 채움)

---

## 8. 이번 과제에서 실패한 것들 (반복 금지)

- **`ent_coef=0.05` 과다**: 관측이 rich한데도 탐색 강화 → 93점 손실
- **초기 `R_FINISH=0`**: 이탈 억제만 되고 대기 과잉 → 100점 손실
- **`gamma=0.999` + 양수 보너스**: 발산 → 점수 2배 이상 악화
- **verify_env.py의 garage_3 FAIL을 오해**: manifest가 구버전이라 발생하는 정상 상황을
  파일 훼손으로 오독할 뻔했음. 배포 시점을 먼저 확인할 것.
- **200k → 500k 이동 시 병렬로 전환**: 병렬 오버헤드로 individual 처리량 저하.
  중요한 결정적 실험은 단일 프로세스로.
- **파일 공유 제약 관측 v1 → v2 전환 후 v1 산출물 평가 시도**: OBS_DIM 불일치.
  관측을 바꾸면 이전 sweep 모델은 모두 폐기됨을 명심.
